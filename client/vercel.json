{
  "framework": "nextjs",
  "installCommand": "cd ../opgrapes && npm ci --no-audit --no-fund --include=dev",
  "buildCommand": "cd ../opgrapes && npm run build",
  "outputDirectory": "../opgrapes/apps/web/.next",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}

