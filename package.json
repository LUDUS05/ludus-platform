{
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "npm ci --prefix client && npm run build --prefix client",
    "start": "npx serve -s client/build",
    "setup-demo-users": "node scripts/setup-demo-users.js",
    "demo-users": "npm run setup-demo-users"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "eslint": "^8.57.1",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "mongodb-memory-server": "^10.1.4",
    "shadcn": "^3.2.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "axios": "^1.12.2"
  }
}
